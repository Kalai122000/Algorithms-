import java.util.Scanner;

public class Main {
    public static int binarySearch(int arr[], int left, int right, int x) {
        int mid;
        while (left <= right) {
            mid = (left + right) / 2;
            if (arr[mid] == x)
                return mid;
            else if (arr[mid] < x)
                left = mid + 1;
            else if (arr[mid] > x)
                right = mid - 1;
        }
        return -1;
    }

    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        int[] arr = new int[9];
        int output;
        
        for (int i = 0; i < 9; i++) {
            arr[i] = scanner.nextInt();
        }

        output = binarySearch(arr, 0, 8, 20);
        if (output == -1)
            System.out.println("Element not found");
        else
            System.out.println("Element present at location: " + output);
    }
}
